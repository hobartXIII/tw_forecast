# 複製成 .env.local（不會被 commit）後填入。VITE_ 開頭的變數會打包進瀏覽器，只能放 anon key。
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=

# 只給伺服器端（api/）用，不可加 VITE_ 前綴。本機 `npm run dev` 也會讀這幾個值；
# SUPABASE_URL / SUPABASE_ANON_KEY 沒填時沿用上面的 VITE_ 值。
GH_REPO=hobartXIII/tw_forecast
GH_DISPATCH_TOKEN=
